[tool.ruff]
line-length = 120
target-version = "py38"
exclude = ["tests/fixtures"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[[tool.mypy.overrides]]
module = ["LSP.*", "sublime", "sublime_plugin"]
ignore_missing_imports = true
